//lets trigger the jenkins and see the result for this basic pipeline code

pipeline
{

    agent any

    stages {

        stage('checkout'){

        }
        
        
        stage('build'){
        }

        stage('Docker Build'){
        }

        stage('Deploy'){
        }
    }
}
